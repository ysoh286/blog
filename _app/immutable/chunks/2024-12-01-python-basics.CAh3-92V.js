import{a as T,t as x}from"./disclose-version.hD3RjQbW.js";import"./legacy.C6sgJd9a.js";import{s as t,f as A,c as e,r as a,n as c}from"./runtime.Bs2-O94s.js";import{h as o}from"./html.DBamY79b.js";const O={title:"Weekly learning review - python basics",date:"2024-12-01T20:00:00.000Z"};var N=x('<h2>This week’s progress:</h2> <ul><li>Started and completed Nina Zakharenko’s Practical Guide To Python (mostly a beginner refresher, but a few quirks learnt about python)</li></ul> <h2>Summary:</h2> <p>On basic python with quirks and gotchas:</p> <ul><li><code>import antigravity</code> opens up kcxd comic about python (easter egg)</li> <li><code>import this</code> prints out the zen of python (which is so good from a code standpoint)</li> <li><code>None</code> is valid (it’s similar to <code>null</code> in JS)</li> <li>A list is similar to that of JS’s array <code>[]</code> / <code>list()</code></li> <li>Sets are used to identify unique values - use <code>set()</code> to create a set</li></ul> <pre class="language-undefined"><!></pre> <ul><li>Tuples are represented using parentheses and are immutable (cannot be updated like a list, have order) <ul><li>Notes that the comma and the parentheses makes the tuple, not just the parentheses</li></ul> <pre class="language-undefined"><!></pre> <ul><li>we can unpack tuples provided we match the number of values inside the tuple</li></ul> <pre class="language-undefined"><!></pre></li> <li>Dictionaries are used to store data in key value pairs (like objects in JS) <ul><li>keys must be immutable data types</li> <li><code>&#123;&#125;</code> / <code>dict()</code> to declare a dictionary</li> <li>cannot use indices to access values, and rather use keys to access values</li></ul> <pre class="language-undefined"><!></pre> <ul><li>To delete, there is <code>del</code> but we’re better off using <code>pop</code> instead</li></ul></li> <li>Do NOT use mutable types as default arguments for functions. The reason for this is that the argument only gets declared and instantiated once under the hood and continues using that same instance. Rather - if you need to have a new value every time, define it inside the function rather than as an argument. <ul><li>do NOT put lists as an argument, use a placeholder argument instead (such as <code>None</code>)</li></ul></li> <li>Use <code>is</code> and <code>is not</code> when you need to check equality with boolean values such as <code>None</code>, <code>True</code>, <code>False</code> rathe r than <code>==</code> and <code>!=</code> <ul><li>favour <code>5 is True</code> over <code>5 == True</code></li></ul></li> <li>Use <code>enumerate</code> to loop over a list and its index - this returns a list of tuple values with index as the list and value at that index in that list</li></ul> <pre class="language-undefined"><!></pre> <ul><li>To loop over a dictionary, use a for loop and loop over <code>dict.items()</code></li></ul> <pre class="language-undefined"><!></pre> <ul><li>List comprehensions, dictionary comprehensions are ways to help go through a list and return an output of either a list or dictionary based on those values <code>[what-to-run for value in list]</code> - list comprehension <code>&#123;what-to-run/return-in-key-value-pair for value in list&#125;</code> - dictionary comprehension</li></ul> <pre class="language-undefined"><!></pre> <ul><li>A generator is a type of iterable object like a list but we can iterate through each and return something once</li> <li>Once exhausted, it cannot be used again (state lost) - generally used for optimization and is considered a gotcha as python uses these a bit under the hood to improve memory</li> <li>cannot reference items in a generator without converting it to a list</li> <li>To define exceptions, use <code>try / except</code> blocks, and filter out certain exception / error types for easier debugging</li> <li>We can slice backwards by using negative values (e.g. <code>string_to_slice[-1]</code> will return the last character in <code>string_to_slice</code>)</li> <li><code>__main__</code> and <code>__init__</code> are known as ‘magic’ methods (special methods in python) <ul><li><code>__init__</code> is generally used to define instance variables and as a constructor inside classes</li> <li><code>__main__</code> is used inside modules and is meant to only run once in a standalone program when imported elsewhere</li></ul></li></ul> <h2>References</h2> <ul><li>Nina Zakharenko’s <a href="https://frontendmasters.com/courses/practical-python/introduction/" rel="nofollow">Practical Guide To Python</a> course on FrontEnd Masters</li></ul>',1);function j(f){var u=N(),i=t(A(u),10),v=e(i);o(v,()=>`<code class="language-undefined">unique_values = set(1, 2, 4, 6, 6, 8)
unique_values // 1, 2, 3, 4, 6, 8</code>`),a(i);var n=t(i,2),r=e(n),s=t(e(r),3),y=e(s);o(y,()=>`<code class="language-undefined">(1,) // is a tuple
tuple(,) // is a tuple
() // is also a tuple
1, // is also a tuple (but avoid doing this)</code>`),a(s);var h=t(s,4),_=e(h);o(_,()=>`<code class="language-undefined">colours = (&quot;Red&quot;, &quot;Blue&quot;, &quot;Green&quot;)
red, blue, green = colours</code>`),a(h),a(r);var p=t(r,2),m=t(e(p),3),q=e(m);o(q,()=>`<code class="language-undefined">dict.keys() // returns keys
dict.values() // returns values
dict.items() // returns items</code>`),a(m),c(2),a(p),c(6),a(n);var l=t(n,2),b=e(l);o(b,()=>`<code class="language-undefined">characters = [&quot;Harry Potter&quot;, &quot;Aragon&quot;, &quot;Wednesday Addams&quot;]

enumerate(characters)
list(enumerate(characters))
// [(0, &quot;Harry Potter&quot;), (1, &quot;Aragon&quot;), (2, &quot;Wednesday Addams&quot;)]

for index, character in enumerate(characters):
    print(f&quot;Character found at &#123;index&#125;: &#123;character&#125;&quot;)</code>`),a(l);var d=t(l,4),k=e(d);o(k,()=>`<code class="language-undefined">characters = &#123;
    &quot;HP&quot;: &quot;Harry Potter&quot;,
    &quot;A&quot;: &quot;Aragon&quot;,
    &quot;WA&quot;: &quot;Wednesday Addams&quot;,
&#125;

for key, character_name in characters.items():
    print(f&quot;Character abbreviation &#123;key&#125; with character name: &#123;character_name&#125;&quot;)
</code>`),a(d);var g=t(d,4),w=e(g);o(w,()=>`<code class="language-undefined">// list comprehension
character_names_length = [len(character) for character in characters]</code>`),a(g),c(6),T(f,u)}export{j as default,O as metadata};
